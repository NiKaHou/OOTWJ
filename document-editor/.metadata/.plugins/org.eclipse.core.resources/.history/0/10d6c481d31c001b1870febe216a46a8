package strategy_pattern;

import javafx.stage.Stage;

public class WindowSizeSmall implements WindowSize{

	@Override
	public void setWindowSize(Stage stage) {
		stage.setWidth(1400);
		stage.setHeight(900);
		stage.setResizable(false);
	}
	
}
